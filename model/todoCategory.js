class TodoCategory {
  constructor(id, title, icon, numOfTodos, tintColor) {
    this.id = id;
    this.title = title;
    this.icon = icon;
    this.numOfTodos = numOfTodos;
    this.tintColor = tintColor;
  }
}

export default TodoCategory;
