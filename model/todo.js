class Todo {
  constructor(id, title, description, category, time, isCompleted) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.category = category;
    this.time = time;
    this.isCompleted = isCompleted;
  }
}

export default Todo;
